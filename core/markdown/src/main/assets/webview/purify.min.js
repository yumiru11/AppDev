/*
 * purify.min.js — DOMPurify 浏览器 bundle（资产占位）。
 *
 * ⚠️ 资产待补充：本文件为占位实现（no-op stub），生产构建需替换为真实 DOMPurify bundle。
 *
 * 获取方式（plan.md §2.9 资产准备，assets 打包不从网络加载）：
 *   CDN: https://cdn.jsdelivr.net/npm/dompurify@3.2.0/dist/purify.min.js
 *   npm:  npm install dompurify@3.2.0  →  node_modules/dompurify/dist/purify.min.js
 *
 * 替换步骤：
 *   1. 下载上述 URL 内容
 *   2. 覆盖本文件（保持文件名 purify.min.js 不变）
 *   3. WebViewHtmlBuilder 通过 WebViewAssetLoader 加载此路径
 *
 * 安全：renderer.js 调用 DOMPurify.sanitize(html, config) 做权威清洗；
 * 若本占位未定义 DOMPurify，renderer.js 会跳过 sanitize（Kotlin 端 HtmlSanitizer 已预清洗）。
 */
