/*
 * shiki.min.js — Shiki 语法高亮浏览器 bundle（资产占位）。
 *
 * ⚠️ 资产待补充：本文件为占位实现（no-op stub），生产构建需替换为真实 Shiki bundle。
 *
 * 获取方式（plan.md §2.9 资产准备，assets 打包不从网络加载）：
 *   CDN: https://cdn.jsdelivr.net/npm/shiki@1.24.0/dist/index.unpkg.min.js
 *   npm:  npm install shiki@1.24.0  →  node_modules/shiki/dist/index.unpkg.min.js
 *
 * 替换步骤：
 *   1. 下载上述 URL 内容
 *   2. 覆盖本文件（保持文件名 shiki.min.js 不变）
 *   3. renderer.js 可调用 window.shiki.codeToHtml(code, { lang, theme }) 高亮代码块
 *
 * 仅 OFFLINE_MARKDOWN_IT 模式加载本脚本；SERVER_HTML 模式由 GitHub 服务端已高亮。
 */
