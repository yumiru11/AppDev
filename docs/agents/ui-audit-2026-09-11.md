# AppDev UI 真机截图审计报告（2026-09-11）

> **证据来源**：CI release 真机（模拟器）截图 —— `screenshots-pr199-34600531043`（主证据）、
> `screenshots-pr197-34601029248`、`screenshots-pr196-34601285772`（对照复现）。均为 1080×2400
> 全屏帧，**含状态栏与三键导航栏**（Roborazzi 基线看不到系统栏，故本轮问题只在 CI 真机帧暴露）。
> **审计方式**：`read_image` 逐张读图（**33 张：25 张独立屏 + 5 张拼版 + 3 张跨 release 复现帧**），对照 `docs/ui-design.md`（455 行，权威）
> 与历史清单 `docs/ui-audit-2026-08-21.md`、`FEEDBACK.md`、`CONTEXT.md`。
> **证据复取方式（31MB 图片不入库，避免仓库膨胀）**：
> `gh release download screenshots-pr199-34600531043 --repo yumiru11/AppDev`（主证据），
> 另有 `screenshots-pr197-34601029248` / `screenshots-pr196-34601285772` 供交叉复现。
> **审计边界**：只读审计，未改生产代码、未提交、未 push；未跑 Gradle。
> **方法学声明**：本报告每一条缺陷都注明了「在图上的哪个像素位置看到了什么」。**没有图像证据的条目一律未写入**。
> 交叉验证：所有 P0/P1 均在 pr196/pr197/pr199 三个 release 中**逐帧复现**（非单次抖动）。
> **未跑 Gradle**（含 `verifyRoborazziDebug` / `recordRoborazziDebug`），未改生产代码。

---

## 0. 总览

| 优先级 | 计数 | 含义 |
|---|---|---|
| **P0** | **5** | 内容被遮挡 / 文字不可读（UI-A01 ~ A05） |
| **P1** | **10** | 明显观感或规范违规（UI-B01 ~ B07 + CI 链路 D-01 ~ D-03） |
| **P2** | **12** | 打磨项（UI-C01 ~ C11 + CI 链路 D-04） |
| **合计** | **27** | — |
| 其中「CI 截图链路自身缺陷」 | 4 | D-01 ~ D-04（跑绿灯但没拍到该拍的东西） |
| 已修复确认（历史闭环） | 6 项 | 见 §3 |

**一句话结论**：功能面已经齐了，**唯一系统性硬伤是「底部 insets 避让」**——`Profile` / `Repos` / `code-sora` 三个屏的内容被底栏与系统导航栏实际压住，`code-sora` 与 `notification-panel` 还叠加了 FAB / 遮罩层的二次遮挡，这一组是本轮**必须优先修**的；其次是**搜索结果 tab 行溢出裁切**（最后一个 tab 只能看到 2 个字母）与 **PR 详情 tab 标签被省略号截断**（看不到 "Files changed" 全称）；同时发现 **3 条 CI 截图链路缺陷**——`readme-webview` / `editor` / `commit-dialog` 三张关键帧拍的根本不是它名字对应的屏幕，等于 README 主渲染路径与编辑器链路**当前零 CI 覆盖**。

> ⚠️ **对初审线索的重要更正**：初审列出的「裸英文 `Checking mergeability...` / `Repository not found` 属 i18n 泄漏」经查**不成立**。全部相关文案都在 `values/strings.xml` 里（`pull_request_mergeable_pending`、`repo_error_not_found` 等），`values-zh-rCN/` 也有 177 条对译（`正在检查可合并性…` / `仓库未找到`）。模拟器 locale 是 **en-US**（logcat 中 `en-US` ×2），所以出英文是**正确的 i18n 行为**。详见 §3.6。真正的问题不是「没 i18n」，而是**错误的错误态语义**（UI-B03：README 404 被显示成「仓库不存在」，而该仓库同会话内 issue/PR/文件树全部 200）与**状态 chip 未收敛**（UI-C05）。

---

## 1. 逐屏审计

### 1.1 Home（证据图：`home-light.png` / `home-dark.png` / `board-A-home.jpg` 上排）

- **现状**：状态栏 12:56 + 打孔摄像头 + 电量/信号（y≈50）。顶栏胶囊搜索框 y≈95–210（占宽 ~800px），右侧铃铛 y≈150、头像 y≈150；**铃铛上无未读 Badge**。分区条 y≈290："Feed"（选中，主题色 + 下方 4px 指示条，指示条 x≈135–220）/ "Issues" / "Pull Requests"，三项均匀分布、**右侧未溢出**。内容区："Home" 标题 y≈420，三个通栏长条按钮 y≈490/615/740。feed 区从 y≈870 起是**一个孤零零的 CircularProgressIndicator（y≈935–1045, x≈490–590）**，向下 1200px 直到 y≈2050 全是空白。底栏 y≈2130–2340：Home（选中，secondaryContainer 胶囊 + **实心** home 图标）/ Repos / Profile（未选中，**空心** 图标），y≈2300 为系统三键导航。
- **对照规范**：§3.1「小分区条 + Home 字样 + 长条按钮 + feed + trending」；§8「空态/错误/加载/引导文案全部 i18n」；§5.1「选中实心 / 未选空心**必须做**」。
- **判定**：
  - ✅ 顶部结构与规范一致；长条按钮圆角（≈12dp）、图标 + 文案、通栏；底栏实心/空心语义**已落地**（历史缺陷 #5/#8/#24 确认闭环）。
  - 🔶 **P1｜"Create repository" 图标与其他两个不同源**：`home-dark.png` y≈835 处该图标是一个**实心深色方块**（像被填满的矩形），而同一行的 "Create issue"（y≈710，空心圆 + 点）与 "View pull requests"（y≈935，空心 git-pull-request）都是**细线描边**。三者风格不统一，且实心块在深色下像一块污渍。
  - 🔶 **P1｜feed 首屏加载只有一个裸 spinner**：§3.1 要求「列表首项进入 slide+fade（stagger 24ms）」，§3.2 明确「加载骨架」。当前 1200px 空白 + 单个圈，既无骨架也无文案（"加载动态…"），观感是「卡住了」而不是「在加载」。
  - ✅ 无 emoji 图标；无硬编码色（长条按钮走 surfaceContainer 层级，深浅两套对比正常）。
- **建议修法**：`Create repository` 图标换回 Octicons `repo` outlined 变体（与 `Repos` tab 同一 `AppIcons.Repo`，保证同源）；feed 首载改 `AppLoadingState` 的骨架形态（3–5 个 `ListItem` 灰块 + `shimmer`），保留 `AppPullToRefresh` 用于刷新。

### 1.2 Profile（证据图：`profile.png` + `board-A-home.jpg` 下排左）

- **现状**：顶栏只有 "Profile" 标题（y≈145）+ 右侧齿轮（y≈145）。头像 y≈280–545（大号圆形），昵称 `yumiru11` y≈622，`@yumiru11` y≈692，统计行 y≈790（`23 repos / 0 followers / 2 following / 59 stars`）。Gists 入口行 y≈925（图标 + 文案 + 右箭头）。Tab 行 y≈1065：`Repos`（选中，下划线 x≈0–270）/ `Starred` / `Followers` / `Following` —— **四项横向刚好占满，右侧未裁切**。仓库列表 y≈1160 起，每行 = 圆头像 + 仓库名 + 描述 + `★ 0` + 语言。
- **❌ P0｜最后一行被底栏压住、描述被截断**：y≈2060 处可见 `claude-code` 仓库名，其描述行 `原汁原味 Claude Code 可运行版；Bun 可编译执行版；` 的**后半段直接压在底栏 Home/Repos/Profile 三个标签上**——"Home" 标签与仓库名文字在同一行像素区间（y≈2185）互相叠印，描述文字在 y≈2230 处被切掉。再往下一行（y≈2290）仓库名已被系统三键导航栏盖住，只露出头像上半部。
  - 三个 release 帧（pr196 y=1:15、pr199 y=12:59）**完全一致复现**。
- **对照规范**：§8「48dp 触区 / 对比度达标」；§6.4「insets 走滚动容器的 contentPadding，不是把内容整块 padding 推到栏外」（底栏玻璃必须**糊住**内容，但内容不能与栏内文字同像素叠印——叠印即不可读）。
- **建议修法**：`ProfileScreen` 的 `LazyColumn` 当前 `contentPadding = PaddingValues(bottom = bottomContentPadding)`（`ProfileScreen.kt:226`），需确认 `MainActivity.kt:234` 传入的 `padding.calculateBottomPadding()` 是否等于**底栏实测全高（含 navigationBars，实测 ≈220px/80dp）**；若不等，改为 `bottom = bottomContentPadding + AppDimens.contentPadding`（与 `ReposScreen.kt:244` 同一写法），并加一条 Robolectric 断言测试锁死「最后一项 bottom ≥ 底栏高」。
- **其他**：✅ 统计行/头像/Gists 入口与 §3.5 一致；🔶 见 D-06（统计数字无 AnimatedContent 证据，静态帧无法判定，仅记录）。

### 1.3 Repos 列表（证据图：`repos.png` + `board-B-repo-code.jpg` 左上）

- **现状**：顶栏有搜索胶囊 + 铃铛 + 头像（**与 Home 共用常驻顶栏，符合 §2.1「顶栏跨大分区」**）。"Repositories" 标题 y≈315，右上角**网格/通栏切换按钮** y≈315（4 个方块图标）。列表每行：owner 头像 + Octicons repo 图标 + 仓库名 + `Private` chip（`appdev-api-probe` 行）+ 描述 + `★ 0` + 语言。
- **❌ P0｜底部两行被底栏压住**：最后可见行 `snapdiff`（y≈2090）的 `Shell` 语言标签在 y≈2185 与底栏 `Home` 标签**叠印**；`PiliPlus`（y≈2245）只剩上一行的下表亲，行内容被系统导航栏（y≈2340）与底栏完全覆盖（`board-B` 左上格 y≈745、y≈780 处同样可见两层文字叠在一起）。
- **❌ D-01（P1）｜`reposPage` 完全忽略传入的 padding**：`MainActivity.kt:203-221` 的 `reposPage = { ... }` **丢掉了 `padding` 参数**（对比 `profilePage = { padding -> ... }` 第 226/234 行）。`ReposScreen` 内部虽然自己算 `paddingValues.calculateBottomPadding()`（`ReposScreen.kt:186`），但它拿的是 `ReposScreen` **自己 Scaffold** 的 padding，而 `MainTabPager` 的 `Scaffold.contentWindowInsets = WindowInsets(0.dp)`（`MainTabPager.kt:77`）——**底栏高度不在其中**。这就是 Repos 压字的代码级根因。
- **对照规范**：§3.2「条目：仓库名（粗体）+ 描述（2 行 …）+ 语言色点（GitHub 原色）+ Star/Fork 数 + 更新时间 + 作者头像」；§3.2「网格/通栏两种布局，用户可切换」；§6.4 几何约束。
- **判定**：
  - 🔶 **P1｜owner 头像在每一行重复且完全同图**：若 23 个仓库同属一个 owner，每行左侧都是**同一张头像**（y≈390/625/860/1095/1330/1565/1800），在「我的仓库」语境下是纯噪音。规范 §3.2 要求这一栏承载信息，不是装饰。
  - 🔶 **P2｜Octicons repo 图标在深色下同样呈「实心深色方块」**：`repos.png` 每行 y 处（如 y≈390 x≈240）该图标内部被深色填满，与 `file-tree.png` 里**正确的空心 folder 图标**对比明显不一致。
  - ✅ 语言色点、Star 数、Private chip、视图切换按钮、Empty/Error 三态代码路径齐备。
- **建议修法**：`reposPage` 形参改为 `{ padding -> ReposScreen(..., bottomContentPadding = padding.calculateBottomPadding()) }`；owner 头像在「本人仓库列表」场景**去掉**（或仅首行/分组头显示）；repo 图标统一为 outlined 变体。

### 1.4 仓库详情 · README 空态（证据图：`repo-star.png`(light) / `repo-actions.png`(dark) / `board-B` 右上）

- **现状**：顶栏「← yumiru11/AppDev ⋮」（y≈145）。Tab 行 y≈385：`README`（选中，下划线 x≈45–375）/ `Files` / `Releases`。内容区 y≈540 是一个**浅灰通栏卡片**，里面只有一行 "No README"（x≈45–240），再往下 1600px 全空。
- **对照规范**：§3.8「顶栏（返回 + 仓库名 + 更多菜单）+ **仓库头（名称/描述/Star/Fork/Watch 按钮 + 语言栏）** + 分区 Tab（README / 文件 / Releases）」；§8「空态/错误/加载/引导文案全部 i18n，空态插图用**矢量图标**而非 emoji」。
- **判定**：
  - 🔶 **P1｜仓库头（仓库名/描述/Star/Fork/Watch/语言栏）在整个仓库详情屏不可见**：无论 README / Files / Releases 哪个 tab（`repo-star.png` y≈540–2050、`repo-releases.png` y≈540、`file-tree.png` y≈540、`readme-webview.png` y≈540 全部为空），顶栏下方**直接就是 Tab 行**。§3.8 把这个头部列为页面必备组成。**若这是 #167 「README 下滑收起头部」导致的收起态**——本帧是初始进入（未滚动）状态，头部不应已经收起；**若头部从未实现/被移除**，则是 §3.8 缺件。二者必居其一，需确认。
  - 🔶 **P1｜空态是一个纯文字卡片**：`No README` 只有文本，无矢量图标、无次级说明、无「去创建 README」引导。§8 明确空态要有矢量图标；对比 `notification-panel.png` 的错误态**是有图标 + 文案 + 按钮的**，空态却退化成一个灰底 chip，形态不对称。
  - ✅ README / Files / Releases 三 tab 均可点、选中态清晰；深浅两套对比正常；无 emoji。
- **建议修法**：确认并补齐 §3.8 仓库头（至少：仓库名 + 描述 + Star/Watch/Fork 三按钮 + Linguist 语言栏）；空态套用统一 `AppEmptyState`（矢量图标 + 标题 + 说明 + 可选动作）。

### 1.5 仓库详情 · Releases（证据图：`repo-releases.png` / `board-B` 左中）

- **现状**：Tab 行 y≈385（`Releases` 选中）。次级分段行 y≈540：`Releases`（**选中，实心灰底**）/ `Tags`（描边），右侧 `+ New release`。列表 y≈630 起 6 张卡片，每张：tag 名（`screenshots-pr198-34598256800` y≈700）+ 描述（`CI screenshots …` y≈760）+ `2026-09-11 · github-actions[bot]`（y≈818）。
- **❌ P1｜tag 名与描述几乎无对比度层次**：tag 名（y≈700）与描述文字（y≈760）**亮度几乎一致**，都是中灰；GitHub 上 tag 名是高对比加粗标题。y≈818 的 meta 行是唯一的弱化行。三段文字挤在同一亮度带，层次塌成一层。
- **🔶 P2｜分段按钮选中态对比过低**：`Releases` 选中块（x≈45–275，y≈495–590）的填充色与未选中 `Tags` 的描边几乎不可分辨（见 `pr-diff-unified.png` 里 Unified/Side-by-side 同一问题）。
- **判定**：✅ 列表结构（tag/描述/meta 三行）与 §3.8 一致；✅ `+ New release` 动作可见；🔶 列表末行被导航栏方向裁切（滚动容器，P2）。
- **建议修法**：tag 名提升为 `titleMedium` + `colorScheme.onSurface`（当前疑似 `onSurfaceVariant`）；选中分段块用 `secondaryContainer` 实底 + `onSecondaryContainer` 文字，与未选中拉开 ≥3:1。

### 1.6 仓库详情 · Files 文件树（证据图：`file-tree.png` / `readme-webview.png` / `board-B` 左中下）

- **现状**：`main` 分支 chip（y≈540，圆角矩形描边）+ `+ New file`。列表 `.agents / .cortexkit / .github / .maestro / app / buildSrc / config / core / docs / feature / gradle / prototype / tmp / .editorconfig / .gitignore / AGENTS.md`。
- **判定**：
  - ✅ 文件夹图标**是正确的空心 folder**（y≈640/730/820 …），说明图标链路本身没坏——反证 §1.1/§1.3 的「实心深色方块」是**那两个图标的资源/填充问题**，不是全局 tint 问题。
  - ✅ 文件图标用横线文档图标，与文件夹可区分。
  - 🔶 **P1｜行名用 primary 蓝、按行高 ≈88px 过疏**：每个文件名（`.agents` y≈658、`app` y≈1070 …）都是**主题蓝**，一屏 16 行全是蓝色，主次不分；同时行高 ≈88px（≈32dp 内容的 2.7 倍）导致一屏只能看 16 个文件。
  - 🔶 **P2｜§3.8 要求的「文件/文件夹显示修改时间」在本帧不存在**：每行只有图标 + 名称，无时间列。
- **建议修法**：文件名改 `onSurface`，仅**目录名**保留 `onSurfaceVariant`；行高压到 56–64dp；补修改时间（或明确砍掉该需求并回写 §3.8）。

### 1.7 代码浏览 Sora（证据图：`code-sora.png` / `board-B` 右下）

- **现状**：顶栏「← app/src/main/java… ✏️ 🔍 ⋮≡」。代码从 y≈240 起，行号 1–48，内容为 `MainActivity.kt`。行号约在 x≈50–90，代码从 x≈110 起。
- **❌ P0｜底部内容被 FAB 与系统导航栏双重遮挡**：
  - y≈2000 起，右下角有一块**浮起圆角矩形**（悬浮跳行/搜索按钮，x≈960–1090，y≈1930–2080 区域）**盖住了第 43–46 行代码的右半段**。
  - 最下面一行 `48 import com.yumiru11.githubapp.core.navigation.AppRou`（y≈2320）**被系统三键导航栏拦腰截断**，右侧内容完全不可读——这正是 §9「Android 15+ 强制 edge-to-edge，内容延伸至全屏」下必须用 `contentPadding`/`imePadding` 避让的场景。
- **对照规范**：§3.10「代码：Sora read-only（行号/高亮/**横向滚动**）+ **悬浮跳行/搜索按钮**」；§8「对比度达标」。
- **其他判定**：🔶 **P2｜行号用洋红/紫红色**（x≈50–90 每一行的数字都是紫红），与语法色（关键字紫、类型蓝）撞色，行号本应是最弱的辅助信息；🔶 **P2｜长行被截断而非横向滚动**：第 41/43 行（`import com.yumiru11.githubapp.core.designsystem.token.Loc…`）右侧直接消失，屏幕右缘没有可视的横向滚动提示——§3.10 要求横向滚动可读。
- **建议修法**：Sora 容器上加 `Modifier.navigationBarsPadding()`（或把系统栏高并入 `contentPadding`）；悬浮工具按钮改 `Modifier.align(BottomEnd).padding(bottom = 系统栏高 + 16dp)`；行号改 `onSurfaceVariant`；开启横向滚动并保留最后一行可滚出。

### 1.8 Issue 详情（证据图：`issue-authed.png` / `board-C` 右上）

- **现状**：顶栏「← yumiru11/AppDev #71 ⋮」。第一行 chip 组 y≈300：`● Open`（success 绿实底胶囊）/ `Close`（描边）/ `✏ Edit`（描边）；第二行 y≈458：`🏷 Labels & assignees` / `🔔 Subscribe`。标题 y≈600–740（两行中文）。`yumiru11 opened 3 weeks ago` y≈790。正文 WebView 从 y≈940 起（`此 issue 用于 AppDev 的截图自动化管线验收渲染效果…`），H1/H2/H3/H4 层级清晰，行内代码有主题淡底 + 圆角。右下 FAB `✏ Comment`（x≈690–1035，y≈2140–2260）。
- **判定**：
  - ✅ **状态色语义已修复**（历史缺陷 #4）：`Open` 用 success 绿，与 `Merged` 的 tertiary 紫明显区分——**判定闭环，见 §3.3**。
  - ✅ 正文 WebView 渲染质量高：标题比例、行内代码圆角 + 主题淡底、引用块左侧主题色竖条均符合 §3.11。
  - 🔶 **P1｜`~opened~ 3 weeks ago` 是英文拼装串**：y≈790 处 `yumiru11 opened 3 weeks ago`。在 en locale 下这是对的（见 §0 更正），但该行是**三段拼接**（`{user} opened {relativeTime}`），zh 下要保证语序正确，需确认用的是 plurals/带占位符的整句资源而非 Kotlin 字符串模板拼装。
  - 🔶 **P2｜§3.9 要求的状态区组成不完整**：规范写「StateChip/标题/作者/**标签**/Assignee/Milestone/**ReactionBar**」，本帧只有 StateChip + 标题 + 作者 + 两个动作 chip，**无标签 chips、无 ReactionBar**。
  - 🔶 **P2｜FAB 与内容重叠**：FAB（y≈2140–2260）压住 y≈2320 处的引用块首行 `引用块：这是引用内容 第二行引用` —— 列表末尾若已到底，这段文字永远读不到。
- **建议修法**：FAB 所在 Scaffold 的 `contentPadding.bottom` 需 ≥ FAB 高 + 16dp（`96.dp` 量级），或 FAB 改用 `Modifier.imePadding()` 同源的 inset 计算。

### 1.9 Issue 评论区（证据图：`issue-comments.png` / `board-C` 左上）

- **现状**：滚到正文后半段。可见：一个 3 列**表格**（y≈200–660，`单元格 1 / 单元格 2 / 单元格 3`），`▶ 折叠区域：点击展开`（y≈718），`ⓘ NOTE` 蓝色 Alert（y≈800–920），`⚠ WARNING` 灰底 Alert（y≈975–1095），`GitHub 链接 外部链接`（y≈1155），一张人物图片（y≈1250–1950），右下拉起 FAB `✏ Comment`（y≈1940–2060）。
- **❌ P1｜表格列宽崩溃，中文逐字换行**：表格（x≈130–960）第二列只有 **~50px 宽**，导致 `单元格 2` 竖排成 4 行（y≈220/270/320/370，每行一个字），`带 code 的单元格` 被拆成 `带 / co / de / 的 / 单元 / 格`（y≈490–680）。长文本单元格 `长文本单元格：这是一段比较长的文本用来测试表格换行行为` 反而占了最宽列。**表格当前不可读**。
- **对照规范**：§3.11「表格：GitHub 网页样式（表头加粗 + 上下边框 + 斑马纹 + **横向滚动**）」。当前既无斑马纹，也无横向滚动——而是把列硬挤在屏幕宽内导致逐字断行。
- **判定**：✅ NOTE/WARNING Alert 卡片是「左侧色条 + Octicons 图标 + 标题 + 正文」（符合 §1.2/§3.11，**无 emoji**）；✅ 折叠区域用了 `▶` 三角（矢量，非 emoji）；🔶 **P2｜NOTE 蓝卡白字对比充足，WARNING 卡文字偏灰**（y≈1005 的 `WARNING` 与 y≈1055 正文在深色下对比偏弱）。
- **建议修法**：表格容器加 `Modifier.horizontalScroll(rememberScrollState())` + 每列 `widthIn(min = 120.dp)`，让宽表横向滚动而不是压缩换行；补斑马纹 `surfaceContainerLow` 交替行；WARNING 卡文字提到 `onSurface`。

### 1.10 PR 详情 · Conversation（证据图：`pr-conversation.png` / `pr-actions.png` / `board-C` 左中）

- **现状**：顶栏 #74。chip 行 y≈295：`● Merged`（tertiary 紫实底）/ `Checking mergeability…`（灰底胶囊）。标题 y≈400–600（4 行）。`yumiru11 opened 3 weeks ago` y≈650。`main ← feature/t12-repo-management` y≈710（分支名用等宽字体）。`🔄 Checks pending` y≈798。Tab 行 y≈908：**`Conversa…` / `Commits` / `Checks` / `Files cha…`**，四个 tab 等宽均分。正文 y≈1080 起（含 `Delete branch` 描边按钮 y≈1385、`github-actions[bot]` 评论、`Overall line coverage: 42.2%`、`Download full coverage report` 链接 y≈1735）。右下 FAB `+ Comment`（x≈690–1035，y≈2050–2175）。
- **❌ P1｜Tab 标签被省略号截断，用户读不到完整名**：第 1 个 tab 显示 `Conversa…`（y≈908）、第 4 个显示 `Files cha…`（y≈908 x≈860–1035）。`Files changed` 与 `Conversation` 这两个最常用的 tab **永远显示不全**；`board-C` 三个 PR 屏（#74/#101/#73）**全部复现**。
- **对照规范**：§3.9「PR 额外：PrTabs（Conversation/Commits/Checks/Files changed）」；§8「文案 i18n + 可读性」。当前把 4 个 tab 强制等宽塞进 1080px，等于牺牲了标签可读性。
- **🔶 P2｜`Checking mergeability…` 与 `Merged` 语义矛盾**：同一个 PR **已经是 Merged**（chip y≈295 紫色实底），旁边却常驻一个灰色的「正在检查可合并性…」（x≈290–670）——已合并的 PR 不该在检查可合并性。三张 PR 屏（#74 Merged、#101 Merged、#73 Merged）**全部同时显示这两个 chip**，说明该 chip 恒显、未按状态收敛。
- **🔶 P2｜FAB 压住正文**：FAB（y≈2050–2175）在 `pr-actions.png` 中压住注释 `2. chore(ci)：截图适配 Task…` 的下一行，在 `pr-conversation.png` 中压住 `Screenshot regression (emulator)` 下方的 `home-dark`（y≈2160 处文字被 FAB 覆盖）。
- **✅** `Merged` 状态色（tertiary）与 `Open`（success）区分正确；`Delete branch` 描边按钮形态正确；`**Download full coverage report**` 链接用主题色 + 下划线。
- **建议修法**：Tab 改 `ScrollableTabRow`（或 `PrimaryTabRow` + 标签缩写策略），保证 `Files changed` 全称可读；`Checking mergeability…` chip 仅在 `mergeable == UNKNOWN && state == OPEN` 时渲染；FAB 避让同 §1.8。

### 1.11 PR 详情 · Commits（证据图：`pr-commits.png` / `board-C` 右中）

- **现状**：Tab `Commits` 选中（下划线 x≈275–710）。列表 3 条：`feat(repo): T12 仓库管理 Star/Watch/Fork + Releases/Tags + 语言栏`（y≈825）+ `100d6cf`（y≈900）+ `3 weeks ago`；`chore(ci): 截图脚本加 T12 段（语言栏/Releases Tab）`；`Merge remote-tracking branch 'origin/main' into feature/t12-repo-management`。
- **判定**：
  - ✅ 条目结构（头像 + 提交标题 + short hash + 相对时间）与 §3.9 一致；短 hash 用主题色，可点击语义明确。
  - 🔶 **P2｜大段空白**：3 条提交结束后，y≈1320–2050 是**约 730px 的纯空白**，FAB 孤零零浮在 y≈2050。列表未到底却留出整屏空——观感像加载中断。
  - 🔶 **P2｜Tab 截断同 §1.10**（`Conversa…` / `Files cha…`）。
- **建议修法**：列表底部若不足一屏，用容器居中而非顶对齐；其余同 §1.10。

### 1.12 PR 详情 · Files changed / Diff（证据图：`pr-diff-unified.png` / `pr-diff-side-by-side.png` / `board-C` 下排）

- **现状**：Tab `Files cha…` 选中。文件头 `build.gradle.kts`（y≈885）+ `Modified`（y≈935）+ `+9 −0` + 折叠箭头。分段控件 `✓ Unified` / `Side-by-side`（y≈1015–1120）。unified 帧：hunk 头 `@@ −174,6 +174,15 @@ val coverageExcludes =`（y≈1180）后 12 行，旧/新行号两列（x≈130/250），新增行整行**深蓝底**（y≈1330–1770，`scheme.primaryContainer.copy(alpha = 0.35f)`，见 `PullRequestDiffView.kt:488`，**合规**）。side-by-side 帧：左右双栏，每栏行号 + 代码。
- **判定**：
  - ✅ **Unified / Side-by-side 视图切换已落地**（历史挂账项闭环）；✅ 新增行着色 + 行号双列 + hunk 头灰底均正确；✅ 颜色走 `colorScheme.primaryContainer`，不违反「零硬编码色」。
  - 🔶 **P1｜side-by-side 在手机上不可用（每栏仅剩约 40 字符）**：`pr-diff-side-by-side.png` 中左右栏各自把代码截成 `"**/*Edito…`、`"**/*TabCo…`、`"**/*Timel…`（y≈1300–1400），连 `// T16: 自…` 这样的注释都被砍到只剩 5 个字。hunk 头也退化成 `@@ −174,6 +174,15 @@ va…`（y≈1215）。
  - 🔶 **P1｜unified 视图同样截断而非横向滚动**：`pr-diff-unified.png` 里 `"**/*EditorController*.cla…`、`"**/*EditorController*.cla…`（x≈480–870 后直接消失），屏幕右缘无横向滚动能力。
  - 🔶 **P2｜分段控件选中/未选中对比过低**：`Unified` 选中块（x≈80–540，y≈1015–1120）是**浅灰实底**，未选中的 `Side-by-side` 是描边——两者亮度接近（`repo-releases.png` 的 Releases/Tags 同款问题）。
- **建议修法**：side-by-side 增加每栏 `Modifier.horizontalScroll()`；或在 <600dp 宽度下**隐藏** side-by-side 选项（`repo-releases.png` 同款问题一并处理）；unified 保留横向滚动；分段选中块改 `secondaryContainer` 实底。

### 1.13 Issue 列表 / 仓库内 Issue（证据图：`create-issue.png` / `board-C` 右上）

- **现状**：顶栏「← yumiru11/AppDev 🔍」。筛选行 y≈300：`Open`（**选中**）/ `Closed`（描边）。列表 7 张卡片：标题（2 行，如 `feat(app,datastore,settings): 全局背景图（Part of #167 UI04）`）+ `Open`（主题蓝）+ `yumiru11 opened` + `0 comments`。右下 FAB `✏ New issue`（x≈690–1035，y≈2130–2245）。
- **判定**：
  - 🔶 **P1｜选中 chip（`Open`）与未选中（`Closed`）几乎无法区分**：`Open`（x≈35–210，y≈255–350）填充色仅比背景略深，未选中 `Closed` 是描边——两个状态需要**仔细看才能分辨哪个是当前筛选**。M3 `FilterChip` 选中态应有明确 tonal 填充。
  - 🔶 **P2｜标题被硬截断**：`chore(deps): bump actions/download-artifact from 7 to 8 in the all-actions group across 1 di…`（y≈1390–1465）——截断点落在 `1 di…`，属列表 2 行上限的正常行为，但 2 行内浪费了大量横向空间给英文 scope 前缀，中文标题（`feat(ui): UI 打磨一期…`）也一并被截。
  - 🔶 **P2｜FAB 压住最后一张卡片**：FAB（y≈2130–2245）覆盖了最后一条 `feat(ui): UI 打磨一期·布局与页面` 的行尾（y≈2190 处 `评论 Sheet 完整形态/图片` 被 FAB 遮住）。
  - 🔶 **P2｜状态展示用蓝色文字 `Open` 而非状态色点/chip**：§3.1 要求「状态色点 + 标题 + 仓库名 + 标签 chips + 评论数」，当前是纯文本 `Open`，与 §1.8 详情页的 success 绿不一致。
- **建议修法**：`FilterChip` 选中态改 `FilterChipDefaults.filterChipColors(selectedContainerColor = MaterialTheme.colorScheme.secondaryContainer)`；列表末项 `contentPadding.bottom` 加 `96.dp`（FAB 避让）；状态改 `AppStateChip` 小号形态（success 色点 + `Open`）。

### 1.14 搜索 · 结果 tabs（证据图：`search-tabs.png` / `board-F-search.jpg` 右）

- **现状**：顶栏返回键 + 输入框（内容 `material`，光标可见，右侧 ✕）。Tab 行 y≈310：`Repos`（选中，下划线 x≈18–255）/ `Users`（x≈330–415）/ `Issues`（x≈555–660）/ `PRs`（x≈815–885）/ **`Co…`（x≈1050–1080，只剩 2 个字母的宽度）**。下方 y≈330–500 是**一个左侧开头的 CircularProgressIndicator**（x≈35–150），其余整屏空白。底部软键盘占 y≈1540 以下。
- **❌ P0｜最后一个 tab 被屏幕右缘裁切**：第 5 个 tab 只渲染出 `Co`（`board-F` 右格同样如此），标签文字**在屏幕外**，用户既读不到也无法可靠点击。按 §3.3 的四个结果类型（仓库/用户/Issue/PR）本应只有 4 个 tab，实际有 5 个且第 5 个溢出。
- **对照规范**：§3.3「结果 Tabs（仓库/用户/Issue/PR）」；§8「48dp 触区 / 可读性」。tab 行**必须可横向滚动**（`ScrollableTabRow`），当前是被固定宽度容器裁掉。
- **🔶 P1｜结果加载态是一个裸 spinner，无骨架无文案**：y≈330–500 只有一个圈，紧贴屏幕左缘（x≈35），**且与 tab 行下划线几乎相接**，视觉上像误画的元件。§3.3 要求「输入防抖 300ms 后结果区 Crossfade」，§3.2 要求骨架。
- **建议修法**：tab 行改 `ScrollableTabRow`（`edgePadding = 16.dp`），保证第 5 个 tab 可滚入视野；确认第 5 个 tab 是否应存在；结果区改骨架列表 + `LoadingState` 文案。

### 1.15 搜索 · 建议 chips（证据图：`search-history.png` / `board-F` 左）

- **❌ P1｜`search-history` 帧里没有任何历史记录**：屏幕上只有 `Suggestions` 标题（y≈348）+ 6 个 qualifier chip（`is:issue` / `is:pr` / `is:open` / `language:kotlin` / `stars:>100` / `user:`，y≈440 与 y≈575 两行），**没有「历史记录」区，也没有清空历史的入口**。§3.3 明确「大号搜索框（自动聚焦）+ **历史记录 chips（可清空）**+ qualifier 建议 + 结果 Tabs」，且「**空搜索 → 历史**」。
  - 该帧文件名就是 `search-history.png`，对照 `search-tabs.png`（有输入 `material`）——说明这帧确实代表「空搜索态」，而空搜索态**只给了 Suggestions**。
  - 需区分两种可能：(a) 历史功能未实现/未接线；(b) 历史为空（`clearState: true` 冷启）时无空态占位。无论哪种，「可清空的历史记录」这一节在当前 UI 中**不可见**。
- **判定**：✅ qualifier chips 用等宽字体 + 主题色，可点性明确，符合 §3.3；✅ 空态无 emoji、无英文泄漏。
- **建议修法**：补「最近搜索」分组（`FlowRow` chips + 右侧「清空」TextButton）；历史为空时给该节一个 `AppEmptyState` 占位（或明确不渲染该节并回写 §3.3）。

### 1.16 通知面板（证据图：`notification-panel.png` / `board-E` 右上）

- **现状**：面板从顶部铺满全屏。「Notifications」标题 y≈145（**被下层 Home 的搜索框 `Search GitHub…` 文字穿透叠印**）。同一行右侧：`Filter`（主题蓝，y≈145）/ `Mark all read`（y≈145，**文字上叠着一个圆形图标**）/ `✕`（y≈145，贴屏幕右缘、被裁）。第二行 y≈320：`All`（选中实底）/ `Participating` / `Newest`（选中，带 ✓）/ `Oldest` —— **且下层 Home 的分区条 `Issues`/`Pull Requests` 文字同时穿透可见**。错误态 y≈400–780：三角警告图标 + `Sign-in expired. Please sign in again.` + `Retry`（primaryContainer 胶囊）。列表 y≈950 起 6+ 条：`lsc188zq starred / shiroha-233/RoadWeaver / 3 days ago`、`GuoqiFish pushed 0 commits / xingji-studio/OpenXJ380 / 1 week ago`（×2）、`GuoqiFish updated pull request #80`、`GuoqiFish opened pull request #80`、`GuoqiFish closed pull request #79`。下层 Home 的三个长条按钮（`Create issue` y≈528 / `View pull requests` y≈655 / `Create repository` y≈782）与底栏（`Home`/`Repos`/`Profile` y≈2130）**全部穿透可见**。
- **❌ P0｜面板内容与下层 Home 同像素叠印，双向不可读**：
  - **标题行**：「Notifications」（白色大字，y≈145）与下层 `Search GitHub…`（y≈145）在同一像素行叠印；
  - **筛选行**：`All / Participating / Newest / Oldest` 与下层 `Issues`（y≈200 附近）叠印；
  - **列表最末项**：`GuoqiFish updated pull request #73`（y≈2180）压在底栏 `Home` 标签（y≈2185）上，其 `xingji-studio/OpenXJ380 / 1 week ago`（y≈2230）压在 `Repos`/`Profile` 标签上；
  - 结果：**面板标题、筛选行、列表末项三处都读不清**。
- **对照规范**：§3.4「**遮罩**：中浅遮罩（背后隐约可见约 50%）+ **面板自身玻璃**」——「背后隐约可见」指的是**遮罩层**，不是**面板内容与背景文字同像素叠印**。§6.4 的玻璃语义是「内容在栏**后面**滚动穿过」，即栏内文字必须**不透明可读**。
- **其他判定**：
  - 🔶 **P1｜`Mark all read` 文字与图标重叠**：y≈145 处 `Mark all read` 的字符中间叠了一个圆形图标（约 x≈680），二者互相破坏。
  - 🔶 **P2｜`✕` 关闭按钮贴屏幕右缘被裁**：x≈1040–1080，右侧约 8px 在屏幕外，按 §8「48dp 触区」其可点区已被压缩。
  - 🔶 P2｜列表项被 `SCRIM_ALPHA = 0.75f`（`AppBlur.kt:28`）统一压低亮度，未读/已读的 left-primary 色条区分不明确。
  - ✅ 错误态**有矢量图标 + 文案 + Retry 按钮**（形态正确，符合 §8；`Retry` 用 primaryContainer）；✅ 用了 `SingleChoiceSegmentedButtonRow` / `FilterChip`，选型符合 M3。
- **建议修法**：通知面板自身需要**不透明底**（或提高 scrim alpha / 在面板内容层加 `surface` 实底），保证标题/筛选/列表不与下层叠印；面板 `contentPadding.bottom` 需含 `navigationBars`（末项不该压底栏）；`Mark all read` 改 `IconButton + contentDescription`（或 `TextButton` 纯文字），不与图标同居一行；`✕` 加 `end padding 8.dp`。

### 1.17 设置页（证据图：`settings-grouped.png` / `board-E` 左上）

- **现状**：只有 "Settings" 标题（y≈145），**顶栏无返回箭头**。`Appearance` 分组头 y≈295（主题蓝）。分组卡 1：`Theme / System` + `System`（选中实底）/ `Light` / `Dark` 三按钮；`Dynamic color`（开关 off）；`Seed color / Blue` + **8 个色圆**（y≈1000 一行 7 个 + y≈1135 一个，x≈95–960）；`OLED pure black`（off）；`High contrast`（off）；`Glass effect`（**on**，蓝色满轨道）+ 子项 `Top bar`（on）/ `Bottom bar`（on）/ `Notification panel`（on）。
- **判定**：
  - ✅ **分组卡片形态已落地**（外圆角大卡 + 组头在卡外/卡上），符合 §3.6「分组卡片（不同分组用不同卡片，分组头不需要玻璃）」——历史挂账项闭环。
  - ✅ **玻璃逐项开关已落地**（Glass effect 总开关 + Top bar / Bottom bar / Notification panel 三个子开关），符合 §6.3「全局总开关 + 设置页逐项开关」——历史挂账项闭环。
  - ✅ 开关 on 态用 primary 实底（y≈1665/1785/1905），与 off 态（y≈700/1300/1445）区分明确；无硬编码色。
  - 🔶 **P1｜无返回箭头**：y≈145 左侧空白，屏幕内**没有可见的返回入口**。用户从 Profile 齿轮进来后，只能靠系统返回手势/按键。§3.6 未单列顶栏，但 §8 与 M3 二级页惯例要求可达；对比所有其他二级屏（repo / issue / PR / search）**都有 ← 箭头**，设置页独缺。
  - 🔶 **P2｜Seed color 色圆触区偏小**：8 个圆直径约 70px（≈25dp），间距约 95px。§8 要求 48dp 触区（历史缺陷 #8 同款问题，未闭环）。
  - 🔶 **P2｜分组卡内子项文字缩进但开关未对齐**：`Top bar`（y≈1790，x≈195）与右侧开关（y≈1790 中心）尚可，但组内分隔线的左右边界与新组卡片不一致，视觉分块略乱。
- **建议修法**：补 `TopAppBar` + `navigationIcon = IconButton(Icons.AutoMirrored.Filled.ArrowBack)`；色圆套 48dp `Box` 点击区（`Modifier.size(48.dp).clickable{}.padding(11.dp)` 画出 26dp 圆）。

### 1.18 阅读 README 深链（证据图：`editor.png` / `commit-dialog.png` / `board-B` 左下 / `board-E` 左下）

- **现状**：顶栏「← **README.md**」。整屏空白，y≈1200 处一行**粉/红色文字 `Repository not found`**，y≈1330 处一个 `Retry` 胶囊（primaryContainer 浅蓝 + 深色文字）。
- **❌ P1｜错误语义错误 + 无法区分是「仓库不存在」还是「README 不存在」**：
  - 用户是从**存在且可访问**的 `yumiru11/AppDev` 进来的（本次会话里同一个仓库的 issue #71、PR #74、文件树、Releases 全部 200 成功）。
  - logcat 给了确凿证据：
    - `12:57:34.926 --> GET https://api.github.com/repos/yumiru11/AppDev/readme` → `12:57:35.148 <-- 404`（pr199 logcat:21142-21143；pr196 的 `12:59:18` 同样 404）
    - `12:57:50.125 --> GET …/contents/README.md?ref=main` → `<-- 404 (149ms, 127-byte body)`（logcat:21407-21408）
    - **对照成功**：`…/contents/app%2Fsrc%2F…%2FMainActivity.kt?ref=main` → `<-- 200 (46482-byte body)`（logcat:21580-21601）
    - `readme-render-log.txt` 里**只有** `repo=mermaid-js/mermaid renderMode=WEBVIEW` 两条，**AppDev 的 README 失败路径没有任何 ReadmeRender 日志**（静默吞掉）。
  - `README.md` 在 `file-tree.png` 里**明确列在文件树顶层**（y≈1010），所以「仓库不存在」这个文案是错的——真正缺的是 README 内容。
  - 因此：**`repo_error_not_found` 被错误地复用到 README 加载失败上**，文案对用户是误导（用户会以为仓库被删了）。
- **❌ P1｜该深链链路整体失效，连带 `editor.png` / `commit-dialog.png` 两个截图全部拍到了这个错误屏**（见 D-02/D-03）。
- **对照规范**：§8「空态/错误/加载/引导文案全部 i18n」+ 语义正确；§3.11「README：服务端 HTML 优先（getReadmeHtml 三级降级 + 双 key 缓存）；服务端异常 → **离线 GFM markdown-it 降级**」。当前 404 后**没有落到离线降级**，而是直接进错误屏。
- **建议修法**：`getReadmeHtml` 404 分支区分「仓库 404」与「README 404」——后者应走离线 GFM 降级（`contents` 也 404 时再报空态，文案改「该仓库暂无 README」）；`Retry` 按钮改 `Button`（primary 实底）以符合 M3 最高层级动作，或明确降为 `TextButton`；失败路径补 `ReadmeRender` 日志（当前静默，CI 无从定位）。

### 1.19 编辑 / 提交对话框（证据图：`editor.png` / `commit-dialog.png`）

- **状态**：**未获取到有效证据**。两张图（pr199 md5 `44b5911…` / `bd05e68…`，字节数均为 43618；pr196/pr197 两张 md5 **完全相同** `2d93e91a…`）实际内容都是 §1.18 的 README 错误屏。
- **判定**：**❌ P1（D-02/D-03）——「Markdown 编辑器」与「提交对话框」当前零 CI 视觉覆盖**，无法评估其布局/主题/避让是否符合 §3.10、§3.9 D2-3。
- 详见 §2 的 D-02 / D-03。

### 1.20 README 渲染（mermaid 路径）（证据图：`readme-mermaid.png`）

- **现状**：顶栏「← mermaid-js/mermaid ⋮」。Tab 行 y≈385（`README` 选中，下划线 x≈45–375）。内容 y≈700 起：**居中大标题 "Mermaid"**（y≈660–740）+ 一条水平分隔线（y≈915）+ **居中一句话 "Generate diagrams from Markdown-like text."**（y≈845–890）。y≈950 之后**整屏空白直到 y≈2200**；屏幕最右缘 y≈390–520 有一小段灰色**滚动条残影**。
- **关键交叉验证**：logcat `GitHubRest <-- 200 https://api.github.com/repos/mermaid-js/mermaid/readme (247ms)`（logcat:20725）+ `ReadmeRender: repo=mermaid-js/mermaid renderMode=WEBVIEW lines=485 bytes=24957`（logcat:20726）——**服务端返回了 485 行 / 24957 字节的 README，且判定为 WEBVIEW 模式**。
- **判定**：**❌ P2（UI-C11）｜485 行内容只画出标题 + 一句话 + 分隔线**：数据到位、渲染模式正确，但视口内只呈现了 README 的头部。右缘那段孤立滚动条进一步说明「内容高度」与「WebView 视口高度」不一致。
  - ✅ 这部分**至少证明 WebView 主渲染路径在 mermaid 仓库上没有崩**（对比 AppDev 的 `Repository not found`，见 §1.18/UI-B03），即 §3.11 的两级降级链路本身是活的。
- **建议修法**：检查 WebView `layoutParams` / 高度测量时序（服务端 HTML 首帧注入后内容高度变化未触发布局更新）；在截图链路加断言「渲染完成后内容高度 > 视口高度」。

### 1.21 拼版 board（证据图：`board-A-home.jpg` / `board-B-repo-code.jpg` / `board-C-issue-pr-diff.jpg` / `board-E-settings-notif.jpg` / `board-F-search.jpg`）

- **判定**：5 张拼版（`montage_board`，见 `.github/scripts/screenshots.sh:466-469`）作为回归速览**有效**——`board-B` 一格就同时暴露了「repos 底部压字（左上 y≈745/780 两层文字叠印）」「仓库详情无仓库头（右上全空）」「Releases/文件树行高与颜色（左中）」「code-sora 底部遮挡（右下）」。
- 🔶 **P2｜拼版命名与内容不符**：`board-A-home.jpg` 的 4 格是 `home-light / home-dark / profile / (空)`，`board-E-settings-notif.jpg` 含 `settings-grouped / notification-panel / commit-dialog / (空)`，`board-B` 第 6 格（`editor.png`）是空白——**拼版把三个坏帧（readme-webview/editor/commit-dialog）当成有效格留在板上**，看板本身失去了对这些路径的回归能力。

---

## 2. 缺陷清单（按优先级排序）

| ID | 优先级 | 屏 | 缺陷 | 证据图 | 对照条款 | 建议修法 |
|---|---|---|---|---|---|---|
| **UI-A01** | **P0** | Profile | 仓库列表末行被底栏 + 系统导航栏压住，描述文字被截断（`claude-code` 行文字与底栏 `Home` 标签同像素叠印） | `profile.png`(y≈2060–2290) · `board-A` 下排左 | §6.4 / §8 | `ProfileScreen` 的 `LazyColumn` bottom padding 加 `AppDimens.contentPadding`，并断言 ≥ 底栏全高 |
| **UI-A02** | **P0** | Repos | 末两行被底栏压住（`snapdiff` 与 `PiliPlus` 的语言标签/行文字压底栏标签）；代码级根因：`MainActivity.kt:203` 的 `reposPage` 丢弃了传入的 `padding` | `repos.png`(y≈2090–2290) · `board-B` 左上 | §3.2 / §6.4 | `reposPage = { padding -> ReposScreen(..., bottomContentPadding = padding.calculateBottomPadding()) }` |
| **UI-A03** | **P0** | 搜索 | 第 5 个 tab 被屏幕右缘裁切，只显示 `Co`（读不到、难点到） | `search-tabs.png`(x≈1050–1080) · `board-F` 右 | §3.3 / §8 | tab 行改 `ScrollableTabRow` + `edgePadding`，并确认第 5 个 tab 是否应存在 |
| **UI-A04** | **P0** | 通知面板 | 面板标题（与下层 `Search GitHub…` 叠印）、筛选行（与下层 `Issues` 叠印）、列表末项（压底栏）三处同像素叠印不可读 | `notification-panel.png`(y≈145/200/2180) · `board-E` 右上 | §3.4 / §6.4 | 面板内容层加不透明 `surface` 底；面板 bottom padding 含 `navigationBars` |
| **UI-A05** | **P0** | 代码浏览 | 第 43–46 行被右下悬浮按钮盖住；最后一行被系统导航栏拦腰截断不可读 | `code-sora.png`(y≈1930–2080 / y≈2320) · `board-B` 右下 | §3.10 / §9 | Sora 容器加 `navigationBarsPadding()`；悬浮按钮 bottom 偏移加系统栏高 |
| **UI-B01** | **P1** | 搜索 | `search-history` 帧完全没有历史记录区，只有 `Suggestions`；§3.3 的「历史 chips（可清空）」与「空搜索 → 历史」不可见 | `search-history.png`(全屏) · `board-F` 左 | §3.3 | 补「最近搜索」分组 + 清空入口；历史为空时给空态占位 |
| **UI-B02** | **P1** | PR 详情 | Tab 标签被省略号截断：`Conversa…` / `Files cha…`，最常用两个 tab 永远读不全 | `pr-conversation.png`/`pr-actions.png`/`pr-commits.png`(y≈908) · `board-C` 三处 | §3.9 / §8 | 改 `ScrollableTabRow` 或标签缩写策略，保证 `Files changed` 全称可读 |
| **UI-B03** | **P1** | README 深链 | `/readme` 404 被映射为「Repository not found」，但该仓库存在（同会话 issue/PR/文件树全 200）；且未走 §3.11 的离线 GFM 降级，失败路径无 `ReadmeRender` 日志 | `editor.png`/`commit-dialog.png`(y≈1200) · `board-B` 左下 · logcat:21142/21407 | §3.11 / §8 | 区分「仓库 404」与「README 404」；后者走离线降级；补失败日志 |
| **UI-B04** | **P1** | Issue 评论 | 表格列宽崩溃，中文逐字换行（`单元格 2` 竖排 4 行、`带 code 的单元格` 拆成 6 行），表格不可读 | `issue-comments.png`(y≈200–680) · `board-C` 左上 | §3.11「表格…横向滚动」 | 表格容器 `horizontalScroll` + `widthIn(min=120.dp)`；补斑马纹 |
| **UI-B05** | **P1** | Diff | side-by-side 在手机上每栏只剩 ~40 字符（`"**/*Edito…`）；unified 同样截断且无横向滚动 | `pr-diff-side-by-side.png`(y≈1215–1400) · `pr-diff-unified.png`(y≈1240–1770) | §3.10 | 每栏加横向滚动；或 <600dp 隐藏 side-by-side |
| **UI-B06** | **P1** | Issue 列表 | 选中 `Open` chip 与未选中 `Closed` 几乎无法区分（tonal 填充过淡） | `create-issue.png`(y≈255–350) · `board-C` 右上 | §3.1 / M3 FilterChip | 显式设 `selectedContainerColor = secondaryContainer` |
| **UI-B07** | **P1** | 设置 | 整个屏没有返回箭头（其他所有二级屏都有 ←） | `settings-grouped.png`(y≈145 左侧空白) · `board-E` 左上 | §8 / M3 二级页惯例 | 补 `TopAppBar` + `navigationIcon` 返回按钮 |
| **UI-C01** | **P2** | 仓库详情 | 仓库头（名称/描述/Star/Fork/Watch/语言栏）在 README/Files/Releases 三个 tab 均不可见，只剩 Tab 行 | `repo-star.png`/`repo-releases.png`/`file-tree.png`(y≈540) | §3.8 | 确认是否 #167 收起态误触发；否则补齐 §3.8 仓库头 |
| **UI-C02** | **P2** | 仓库详情 | 「No README」空态是纯文字灰卡，无矢量图标 / 无次级说明 / 无引导动作 | `repo-star.png`(y≈540) | §8 | 套统一 `AppEmptyState`（矢量图标 + 标题 + 说明 + 动作） |
| **UI-C03** | **P2** | Releases | tag 名与描述亮度几乎一致，层次塌成一层 | `repo-releases.png`(y≈700/760) | §3.8 | tag 名提到 `titleMedium` + `onSurface` |
| **UI-C04** | **P2** | Releases/PR Diff | 分段按钮选中/未选中对比过低 | `repo-releases.png`(y≈540) · `pr-diff-*.png`(y≈1015) | M3 SegmentedButton | 选中块改 `secondaryContainer` 实底 |
| **UI-C05** | **P2** | PR 详情 | `Checking mergeability…` 与 `Merged` 同时显示（三个 PR 全中），chip 未按状态收敛 | `pr-conversation.png`/`pr-actions.png`/`pr-commits.png`(y≈295) | §3.9 | 仅 `mergeable==UNKNOWN && state==OPEN` 时渲染 |
| **UI-C06** | **P2** | Issue/PR 详情 | FAB 压住正文（引用块首行 / 正文行尾），列表到底后永远读不到 | `issue-authed.png`(y≈2320) · `pr-actions.png`(y≈2190) · `create-issue.png`(y≈2190) | §3.9 / §8 | FAB 所在 Scaffold `contentPadding.bottom` ≥ 96dp |
| **UI-C07** | **P2** | Files 文件树 | 文件名全部用 primary 蓝、行高 ≈88px 过疏（一屏仅 16 行）；§3.8 要求的修改时间缺失 | `file-tree.png`(y≈640–2020) | §3.8 | 文件名改 `onSurface`，行高压到 56–64dp；补修改时间 |
| **UI-C08** | **P2** | 代码浏览 | 行号用洋红/紫红与语法色撞色（应为最弱辅助信息）；长行截断而非横向滚动 | `code-sora.png`(x≈50–90 / y≈900–1450) | §3.10 / §8 | 行号改 `onSurfaceVariant`；开启横向滚动 |
| **UI-C09** | **P2** | 设置/通知 | Seed 色圆触区 ≈25dp（<48dp）；通知面板 `Mark all read` 文字与图标重叠、`✕` 被右缘裁切 | `settings-grouped.png`(y≈1000) · `notification-panel.png`(y≈145) | §8 | 色圆套 48dp 点击区；`Mark all read` 拆分图标与文字；`✕` 加 end padding |
| **UI-C10** | **P2** | Repos/Home | `Create repository` 与每行 repo 的 Octicons 图标在深色下呈「实心深色方块」，与 `file-tree.png` 的空心 folder 不一致；Home feed 首载只有一个裸 spinner | `home-dark.png`(y≈835) · `repos.png`(y≈390 等) · `home-light.png`(y≈990) | §1.1 / §5.1 / §3.1 | 统一 outlined 变体；feed 首载改骨架 |
| **UI-C11** | **P2** | README(mermaid) | 服务端返回 200（`lines=485 bytes=24957`）但帧上只画出标题 + 一句话 + 分隔线，正文其余全空，右缘悬着一段孤立滚动条残影——WebView 主体高度未撑开 | `readme-mermaid.png`(y≈700–915 有内容 / y≈950–2200 全空) · `readme-render-log.txt` | §3.11「内容宽度左右留白与主页一致」 | 检查 WebView `layoutParams` 高度与 `setContentHeight` 时序；补「渲染完成后内容高度 > 视口」断言 |
| | | | **— CI 截图链路自身缺陷 —** | | | |
| **D-01** | **P1** | CI | `readme-webview.png` 拍到的是 **Files tab 文件树**（不是 README）。脚本在 `tap_text "Files"` 之后才截该帧（`screenshots.sh:315-330`），注释也自认「树下方滚动一屏拍 README 区」——**README 主渲染路径零 CI 回归信号** | `readme-webview.png`(整帧=文件树) · `file-tree.png` 对照 | — | 该帧改回 README tab 后再截（或直接复用 `readme-webview.yaml` 的 mermaid 路径） |
| **D-02** | **P1** | CI | `commit-dialog.png` 拍到了 README 错误屏。`wait_for_text "Describe your changes…"` 带 `\|\| true` 吞掉失败，对话框未出现仍照截 | `commit-dialog.png`(整帧=错误屏) | — | `wait_for_text` 失败应 `exit 1`（或在截图前断言对话框节点存在） |
| **D-03** | **P1** | CI | `editor.png` 与 `commit-dialog.png` 内容相同（pr196/pr197 md5 完全一致 `2d93e91a…`；pr199 同为 43618 字节）——blob 深链失败后 `tap_desc "Edit"` 空转，**编辑器链路零 CI 覆盖** | `editor.png` == `commit-dialog.png` | — | `wait_for_desc "Edit"` 失败即 fail；两张帧加内容差异断言（md5 不同） |
| **D-04** | **P2** | CI | `readme-render-log.txt` 无 AppDev 失败记录（静默吞 404）；拼版 board 把 3 个坏帧当有效格留在板上，看板失去对这些路径的回归能力 | `readme-render-log.txt` · `board-B`/`board-E` 空格 | — | 失败路径补日志；坏帧在拼版上打水印或剔除 |

---

## 3. 已修复确认（历史审计已闭环项）

对照 `docs/ui-audit-2026-08-21.md` 与 `FEEDBACK.md`，以下历史缺陷在本轮真机帧中**确认已闭环**：

| # | 历史条目 | 本轮证据 | 结论 |
|---|---|---|---|
| 1 | **#12 底部导航「仓库」Tab 用 Star 图标** | `home-light.png` y≈2245 的 Repos tab 是 Octicons repo 图标（`AppBottomBar.kt:60` 已换 `AppIcons.Repo`），非 Star | ✅ 闭环 |
| 2 | **FEEDBACK #5/#8/#24 底部图标无实心/空心区分** | `home-light.png`/`profile.png` 底栏：选中项 secondaryContainer 胶囊 + **实心**图标，未选中**空心** | ✅ 闭环（§5.1「必须做」满足） |
| 3 | **缺陷 #4 Issue/PR StatusChip 四状态同用 secondaryContainer，无状态语义色** | `issue-authed.png` 的 `Open`=success 绿；`pr-conversation.png` 的 `Merged`=tertiary 紫——两状态色明显区分 | ✅ 闭环（ExtendedColors 已消费） |
| 4 | **§3.6 设置页「分组卡片」挂账** | `settings-grouped.png` 外圆角大卡 + `Appearance` 组头 | ✅ 闭环 |
| 5 | **§6.3 玻璃「逐项开关」挂账** | `settings-grouped.png` `Glass effect` 总开关 + `Top bar`/`Bottom bar`/`Notification panel` 三个子开关均已接线可点 | ✅ 闭环 |
| 6 | **§3.10 PR「unified/side-by-side 切换按钮」挂账** | `pr-diff-unified.png` / `pr-diff-side-by-side.png` 两个视图均实际渲染，切换控件为 SegmentedButton | ✅ 功能闭环（观感问题见 UI-B05/UI-C04） |

**同时确认四条红线仍然干净**（逐帧核查）：**零 emoji 图标**（Alert 卡片用 Octicons、折叠区用矢量 `▶`、空/错态用三角警告图标）、**零 M2 残留**、**UI 层零硬编码色**（新增行底色走 `colorScheme.primaryContainer`，`PullRequestDiffView.kt:488`）、**错误态有矢量图标 + 文案 + 按钮**（`notification-panel.png`）。

### 3.6 关于「i18n 泄漏」线索的更正（重要）

初审列出的 4 条英文串**全部不构成 i18n 缺陷**：

| 观察到的英文 | 资源条目 | zh-rCN 对译 | 判定 |
|---|---|---|---|
| `Checking mergeability…` | `pull_request_mergeable_pending` / `pull_request_merge_checking_hint` | `正在检查可合并性…` | ✅ 正常 |
| `Checks pending` | `pull_request_checks_state_pending` | `检查进行中` | ✅ 正常 |
| `Delete branch` | `pull_request_delete_branch` / `repo_branch_delete` | `删除分支` | ✅ 正常 |
| `Repository not found` | `repo_error_not_found` | `仓库未找到` | ✅ 正常（**语义**问题见 UI-B03，非 i18n 问题） |
| `Notifications` / `Filter` / `Mark all read` / `New issue` / `Comment` / `Repos` / `Settings` | 各自 `values/strings.xml` | 有 | ✅ 正常 |

模拟器 locale = **en-US**（pr199 logcat 内 `en-US` ×2），`feature/pullrequest/src/main/res/values-zh-rCN/strings.xml` 有 **177 条**对译，双语文案齐全。**本报告的英文出现全部是 en locale 下的正确行为。**
→ 真正需要立项的是 **UI-B03（错误文案语义错误）** 与 **UI-C05（`Checking mergeability…` 状态未收敛）**，而不是「补 i18n」。

---

## 4. 建议新增的 UI 丰富化机会（M3 Expressive 方向，供后续立项）

> 均为本轮真机帧暴露出的**「结构对了但表达还欠一层」**的地方，不阻塞缺陷修复，可独立立项。

| # | 机会 | 现状痛点（证据） | 方向 |
|---|---|---|---|
| 1 | **统一三态组件族落地**：`AppEmptyState` / `AppErrorState` / `AppLoadingState` 真正取代各处手搓 | Home 裸 spinner（`home-light.png` y≈990）、搜索裸 spinner（`search-tabs.png` y≈330）、"No README" 纯文字卡（`repo-star.png` y≈540）、README 错误屏（`editor.png`）**四种形态互不相同** | 用 `LoadingIndicator`（M3E 形变加载）做整页首载、`CircularWavyProgressIndicator` 做行内 pending；空态统一「矢量插图 + 标题 + 说明 + 动作」四件套 |
| 2 | **列表骨架屏（shimmer）** 替代首载 spinner | Home feed 首屏 1200px 空白；搜索结果显示单个圈 | `LazyColumn` 骨架项（`surfaceContainerLow` 圆角块 + 主题色微光），首项进入走 §4.2 的 stagger 24ms |
| 3 | **M3E 动效体系接线**（§4 拍板但零消费，历史缺陷 #5） | 全部帧为静态，无法验证 Star 弹跳 / Reaction 缩放 / 统计数字滚动 / 头像 1.1x 回弹 | `MotionScheme.expressive()` + `LocalMotionScale`（DataStore × 系统 `ANIMATOR_DURATION_SCALE` 取最小）；Star/Reaction 用 `spring(HighBouncy, Medium)` |
| 4 | **`SegmentedButton` 与 `FilterChip` 选中态统一 token** | 三处选中态对比都不足：Issue 列表 `Open`（UI-B06）、Releases `Releases`（UI-C04）、Diff `Unified`（UI-C04） | 在 `core/designsystem` 收口 `AppSegmentedButtonDefaults` / `AppFilterChipDefaults`，选中态一律 `secondaryContainer` + `onSecondaryContainer`，并加对比度单测 |
| 5 | **响应式能力降级：窄屏隐藏不适用视图** | side-by-side diff 在 1080px 上每栏只剩 40 字符（UI-B05） | `WindowSizeClass` 判定：`< Medium` 时隐藏 side-by-side 分段项；宽屏才提供 |
| 6 | **表格横向滚动 + 斑马纹**（§3.11 未落地部分） | Issue 评论表格中文逐字换行（UI-B04） | WebView 侧 CSS 加 `overflow-x:auto` + `tbody tr:nth-child(even)` 底色；并在 Kotlin 侧预计算斑马纹混色变量（真机不支持 `color-mix`） |
| 7 | **通知面板「分组折叠 + 左滑操作」补完**（§3.4 完整形态） | 当前只能看到扁平列表 + `SwipeToDismissBox` 代码存在但帧上无分组头 | 按仓库分组头（可折叠）+ 组内时间排序；未读项左缘 primary 色条（当前被 `SCRIM_ALPHA=0.75` 压平） |
| 8 | **顶栏搜索胶囊的「滚动穿越感」验证**（§6.1 #83 遗留复测项） | 静态帧无法判定玻璃是否真的糊住滚动内容；`repo-star.png` 等帧顶栏下方是纯色 | 在截图链路加一帧**滚动中途**的顶栏特写（内容穿越玻璃矩形的瞬间），把 #167 的真机复测项固化进 CI |
| 9 | **触区审计批次**：Seed 色圆 25dp、通知 `✕`、Reaction chip | UI-C09 | 统一走 `Modifier.minimumInteractiveComponentSize()` + 加一条 Robolectric 触区断言 |
| 10 | **CI 截图看板可信度**：坏帧自动剔除 + 帧内容断言 | D-01~D-04：三张关键帧拍到别的屏，跑绿但零覆盖 | 每张帧加「预期屏幕节点断言」+ md5 去重断言；拼版对缺失帧显式标注 `MISSING` |

---

## 附：审计覆盖清单

**本轮实际用 `read_image` 读取 33 张图像**：

| release | 读图数 | 明细 |
|---|---|---|
| **pr199**（主证据） | **25 张独立屏 + 5 张 board = 30 张**（该 release 的 25 个 `.png` **一张不落全部读完**） | code-sora · commit-dialog · create-issue · editor · file-tree · home-dark · home-light · issue-authed · issue-comments · notification-panel · pr-actions · pr-commits · pr-conversation · pr-diff-side-by-side · pr-diff-unified · profile · readme-mermaid · readme-webview · repo-actions · repo-releases · repos · repo-star · search-history · search-tabs · settings-grouped ＋ board-A-home / board-B-repo-code / board-C-issue-pr-diff / board-E-settings-notif / board-F-search |
| **pr196**（交叉复现，验证非单次抖动） | **4 张** | profile · notification-panel · issue-authed · editor |
| **pr197** | 0 张（仅 `md5sum` 比对 `editor.png` vs `commit-dialog.png`，不重复计入读图数） | — |

**覆盖缺口说明**：任务清单中列名的 `pr-actions` 等 25 个屏名**全部命中并已读**，无遗漏屏。但其中 **3 个文件名与内容不符**（`readme-webview.png` = Files 文件树、`editor.png` / `commit-dialog.png` = README 错误屏），已在 **D-01 / D-02 / D-03** 立项——**「读了图」不等于「读到了该屏」**，这 3 条正是本轮最有价值的发现。

**`readme-mermaid.png` 补充观察**：README 内容**确实渲染成功**（logcat `GitHubRest <-- 200 …/repos/mermaid-js/mermaid/readme` + `ReadmeRender: lines=485 bytes=24957`），帧上可见居中大标题 "Mermaid"（y≈700）+ "Generate diagrams from Markdown-like text."（y≈865）+ 一条水平分隔线（y≈915），**但正文其余部分为空**（y≈950–2200 全空），仅屏幕右缘 y≈390–520 有一段滚动条残影。485 行 README 只画出一屏标题，说明 **WebView 主体高度未撑开**（或内容被裁在视口外）。见缺陷表 **UI-C11**。

**相关性声明**：本报告所有结论均基于上述图像像素观察 + 代码 / logcat 交叉验证；凡无法从静态帧判定的项（动效手感、TalkBack 播报、滚动手感）**一律未列入缺陷表**，仅在 §4 列为待验证方向。
