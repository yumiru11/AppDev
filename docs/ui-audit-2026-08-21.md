# AppDev UI 审查报告（2026-08-21）

> 状态：**已归档**（全面 UI 审查产出，问题清单权威记录）。开发排期与修复流程**另行决定**，本文件不含任何执行计划。
> **2026-08-21 更新**：工程类条目已立项为 GitHub issues **#83-#90**（label `ui-audit`；依赖边 #87/#88/#89/#90 ←blocked_by— #84）；待用户决策项见 §3.0 速览表，不建 issue。
> 审查基准：`docs/ui-design.md`（权威规范）+ ADR-0004/0006/0007 + `FEEDBACK.md` + M3/M3E 官方规范 + 优秀 M3 开源实践（rikkahub CardGroup 等，仅参考思路）。
> 审查方式：全量扫描 `core/designsystem`、`core/ui`、`app`、8 个 feature 模块共 56 个 UI 文件；并按已安装 skills 规则体检（`.agents/skills/`：android-jetpack-compose、android-edge-to-edge、compose-expert、compose-ui、compose-navigation、android-accessibility、compose-performance-audit）。
> 关联：`docs/ui-design.md` §10「UI 修复波票」、`FEEDBACK.md` 待定项、`docs/agents/project-status.md`。

**标记图例**：🔧 代码缺陷（已合入代码有问题，需修复）｜⏳ 已拍板未开发到（设计已定，等对应任务/决策）｜💡 新提案（本次审查新增，原计划没有）｜✅ 合规确认

---

## 1. 总览结论

- 功能进度约 21/26 票（M3 基本收尾），剩 T16/T17/T22/T23/T25（见 project-status.md，注意该文档需更新至 2026-08-21 实况）。
- 四条红线全部干净：零 M2 残留、零 icons-extended、零 RTL 违规、零 emoji 图标、UI 层零硬编码色。
- 主要短板集中三层：
  1. **毛玻璃实现方向错误**（唯一架构级硬伤，见 §2.1）；
  2. **动效系统建而不用**（AppMotion 令牌几乎无消费，§2 缺陷 #5）；
  3. **「已拍板未排票」的 UI 打磨积压**（§3，含 4 项等用户输入的决策）。

---

## 2. 🔧 代码缺陷清单

### 2.1 架构级（P0）

| # | 问题 | 位置 | 说明 / 修复方向 |
|---|---|---|---|
| 1 | **GlassSurface 用 `Modifier.blur` 模糊自身内容**（含顶栏文字/图标/底栏标签），而非背后滚动内容。与 ui-design.md §9 调研结论相悖；即 FEEDBACK #17「毛玻璃仅模糊顶/底栏，非预期」的根因。CI 截图未发现是因 Robolectric 不渲染 RenderEffect | `core/designsystem/.../component/GlassSurface.kt` L66-73 | 改 backdrop 方案（如 Haze：内容侧 `hazeSource` + 栏侧 `hazeEffect`）；API<31 半透明降级策略不变；模糊半径 token 与规范对齐（见缺陷 #17） |
| 2 | **LabelChip 向固定白色混色 45%**：深色主题下标签发白刺眼，文字固定 onSurfaceVariant 对比度不可控 | `feature/issue/.../IssueDetailScreen.kt` L557、`feature/pullrequest/.../PullRequestDetailScreen.kt` L510 | 改 `lerp(labelColor, colorScheme.surface, 0.45f)`，按混合结果亮度动态选 on 色 |
| 3 | **设置页 5 个死设置**：cornerScale / motionScale / iconStyle / codeFont / lineNumbers 有 UI 有 DataStore 持久化、无任何消费点。违反 FEEDBACK #6 先例（未落地前隐藏入口） | `feature/settings/.../AppearanceSettingsSection.kt` | 可接线项：cornerScale→MaterialTheme.shapes、motionScale→全局动画缩放（见 §4 提案 #8）、iconStyle→AppIcon 消费点、codeFont/lineNumbers→Sora 配置；无法接线的先隐藏入口 |

### 2.2 组件级（P1-P2）

| # | 问题 | 位置 |
|---|---|---|
| 4 | Issue/PR StatusChip 四状态（open/closed/merged/draft）同用 secondaryContainer，无状态语义色；ExtendedColors.success/danger 已建但仅 SearchScreen 在消费 | 两 DetailScreen 的 StatusChip |
| 5 | AppMotion 动效令牌全 app 仅 SettingsScreen Crossfade 一处消费；NavHost 仅 NOTIFICATION 一个自定义转场且为默认参数；§4.1 时长/Easing 标准、§4.2 拍板动效（Star 弹跳/stagger/Reaction 缩放/统计滚动/头像回弹）均未实现 | `core/designsystem/.../token/AppMotion.kt`、`core/ui/.../AppNavHost.kt` |
| 6 | Paging key 反模式 ×5：Home/IssueList/PRList 在 key lambda 内调 `lazyItems[index]`（官方明确反模式）；Notifications/Search 退化为纯 index key（破坏 item 状态复用与动画）。现代 `itemKey{}` API 使用数 0 | 五个列表屏 items 块 |
| 7 | `@Immutable/@Stable` 全项目 0 处：FeedItem/NotificationItem 等含 `List<T>` 模型被编译器视为 unstable → 列表行无条件重组风险 | 各 feature model |
| 8 | SeedColorRow 色块完全无 contentDescription（TalkBack 读不出）+ 触点仅 24dp（<48dp 最小触区） | AppearanceSettingsSection.kt L150 一带 |
| 9 | 硬编码圆角散落不走形状令牌：顶栏搜索胶囊 24dp、搜索框 28dp、语言栏 4dp；MaterialTheme 未注入 Shapes（M3 默认恰等于 AppDimens，双事实来源） | AppTopBar.kt L64、SearchTopBar.kt、RepoDetail LanguageBar |
| 10 | 语言栏用主题色循环而非 GitHub Linguist 原色——违反 CONTEXT.md 拍板「语言点 GitHub 原色」，不同语言可能同色 | RepoDetailScreen.kt L569 languageBarColors() |
| 11 | 时间显示分裂：Home feed/通知用绝对日期 yyyy-MM-dd，Issue/PR 列表用 RelativeTime+plurals；RelativeTime 在 issue/pullrequest 双模块重复实现（应上移 core:ui） | HomeScreen formatDate、NotificationsScreen formatDate |
| 12 | 底部导航「仓库」Tab 用 Star 图标，与「收藏」动作语义冲突；规范 §1.2 GitHub 专属语义应用 Octicons repo 图标 | AppBottomBar.kt L54 |
| 13 | ManagementButtons KDoc 称「支持动画过渡」实际 Star/Watch 图标切换为瞬时（文档与实现不符） | RepoDetailScreen.kt L434 一带 |
| 14 | Badge 无 99+ 上限；unread 数未并入铃铛 contentDescription（TalkBack 听不到数量） | AppTopBar.kt L90 |
| 15 | ReactionBar chip 高约 27dp < 48dp 最小触点 | IssueDetailScreen ReactionBar |
| 16 | `@Preview` = 0（Roborazzi 兜底但组件级预览缺失拖慢迭代） | 全局 |
| 17 | 杂项：MaterialYouFusionMapper 散落 `Color(0xFF0B0B0D)` 宜提 token（值为 §3.11 拍板色）；AppBlur.blurRadius=12dp 与规范 §6.3/CONTEXT 的 8dp 文档漂移；NavigationBarItemDefaults.colors 覆盖值恰等于默认值（冗余） | markdown 模块 / AppBlur.kt / AppBottomBar.kt |

### 2.3 无障碍专项（skills 体检发现）

| # | 问题 | 位置 |
|---|---|---|
| 18 | 自定义状态不播报：ReactionBar 已反应态、StatusChip 开/关态、通知未读点均缺 `stateDescription` / 行级 `mergeDescendants` | IssueDetail、NotificationsScreen |
| 19 | 设置分组头（SectionHeader）未标 `semantics { heading() }`，TalkBack 无法按节跳转 | SettingsScreen |
| 20 | LabelChip 任意 label 色 vs onSurfaceVariant 的 WCAG AA（4.5:1）达标性未知（与缺陷 #2 联动修复） | 两 DetailScreen LabelChip |

### 2.4 ✅ 合规确认（验证通过，无需动）

零 M2 残留｜零 icons-extended｜零 RTL 违规｜零 emoji 图标｜UI 层零硬编码色｜edge-to-edge（ComponentActivity.enableEdgeToEdge 自动处理系统栏图标色，无需手动 isAppearanceLight）｜insets 单一来源原则（MainTabPager contentWindowInsets=0 + 各页自处理）｜IME 处理（CreateIssue verticalScroll+imePadding、CommentSheet imePadding）｜UDF 状态架构与 collectAsStateWithLifecycle 全覆盖｜modifier 首参规范抽查合规｜PullToRefreshBox/PrimaryTabRow/ModalBottomSheet/FilterChip/BadgedBox/HorizontalDivider 选型正确｜底部导航 filled/outlined 选中态已实现｜i18n 双语资源全覆盖、plurals 用法正确（issue/PR 模块）。

---

## 3. ⏳ 已拍板未开发到（设计已定，等任务/等决策）

> 本节不是缺陷——均为 ui-design.md / FEEDBACK.md / ADR 已拍板、但对应任务尚未排到或等待用户输入的内容。
> **工程类条目已转为 GitHub issues（label `ui-audit`，#83-#90），进度以 issue 为准；本节保留为静态对照。**

### 3.0 ⏸ 待用户决策速览（不建 issue，拍板后解锁对应条目）

| 决策项 | 阻塞的条目 | 关联 issue |
|---|---|---|
| 图标候选清单批准（Material Symbols rounded/wght300/FILL）| 底栏 repo 图标(#12)、feed/通知事件图标、AppIcon 消费点 | #84 清单行 |
| 卡片 PiliPlus 风格两版对比选型 | 列表行最终形态（surfaceContainer+点缀 vs ListItem 通栏） | — |
| 全局背景图方案 grill 细节 | 背景图入口、主题色渐变兜底取舍 | — |
| 仓库详情 README 头部收起动画构思 | RepoDetail 头部动画两版对比 | — |

### 3.1 等剩余功能票（T16/T17/T22/T23/T25 本体）

| 项 | 依赖票 |
|---|---|
| Diff 渲染 + 行评论 BottomSheet | T16 |
| MergeBox（approve/merge/squash/rebase） | T17 |
| 文件编辑提交（Contents API + 409 冲突 UI） | T22 |
| 分支列表 / 创建 PR | T23 |
| Baseline Profiles、i18n 收尾、签名 Release | T25 |

### 3.2 等「UI 修复波票」（ui-design.md §10 明确挂账）

| 项 | 拍板出处 | 现状 |
|---|---|---|
| 通知面板完整形态：右侧滑入 + 50% 遮罩点击关闭 + 面板玻璃 + 按仓库分组折叠 + 左滑操作 | §3.4 | NotificationPanel 为 T3 占位 stub（顶部滑入/无遮罩/硬编码空态）；真实通知走独立 route |
| 首页小分区条 HorizontalPager 跟手滑动 | §2.1 | 当前点击 Tab 内容瞬切无动画 |
| 卡片 PiliPlus 风格（surfaceContainer 底 + 主题色点缀）+ 网格/通栏布局切换 | CONTEXT.md UI 设计域 / §3.2 | 当前列表行统一 surfaceVariant 平灰；要求「给两版对比」 |
| 玻璃逐项开关（顶栏/底栏/通知面板/BottomSheet 各自可关）+ 背景图设置入口 | §6.3 / §7.4 | T24 仅落总开关 |
| 评论 Sheet 完整形态：编辑/预览切换 + md 工具栏 + 把手 | §3.9 D2-3 | T14 交付简化版（纯输入框+按钮） |
| 列表 stagger 进入动画（设置可开关） | §4.2 H2-2 | 未实施 |
| Star 微缩放+填充动画 | §4.2 H2-7 | 未实施（相关 KDoc 见缺陷 #13） |
| Reaction 点击微缩放 | §3.9 | 未实施 |
| Profile 头像 1.1x 回弹 + 统计数字 AnimatedContent 滚动 | §3.5 | 未实施 |
| 仓库详情 README 头部收起动画 | §3.8 | 等用户构思后两版对比 |
| 图标候选清单批准后换 Material Symbols rounded / wght300 / FILL 语义 | §5.1 / ADR-0004 | **等用户验证批准**；AppIcon 令牌已定义 FILL/wght/ROND 但零消费 |
| Material Symbols 变量字体四轴 | ADR-0004 | 挂账 |
| 全局背景图 | FEEDBACK #30 | 可行性已调研（Compose 层+透明 WebView），**等 grill 细节** |
| md 标题字号校准 | FEEDBACK #11/#23 | 等实机微调 |
| 代码块复制按钮 | FEEDBACK #22 | 新功能未做 |
| 他人主页只读展示 + 关注按钮 | §3.5 | USER 路由已留 login 参数未消费 |
| COMMIT / BLOB 详情页 | 代码注释「T5+」 | 当前渲染 SearchScreen 占位 |
| Trending 穿插 feed（数据源已定：JSON 镜像 + Search API fallback） | §2.5 | 功能票未排 |
| 首页长条按钮 LongBarAction ×3（新建 Issue/查看 PR/新建仓库） | §2.2 | HomeScreen KDoc 注明待 {owner}/{repo} 上下文票补齐 |

### 3.3 用户已决定暂缓

- 平板适配全套：NavigationSuiteScaffold、GridCells.Adaptive、Navigation3 list-detail 双栏、FormFactorPreviews 多设备布局验证（2026-08-21 决定暂不做；多设备 @Preview 注解本身成本极低，可与缺陷 #16 一并考虑）。

---

## 4. 💡 本次审查新增提案（原计划没有，供后续立项参考）

| # | 提案 | 解决什么 |
|---|---|---|
| 1 | Haze 库重做毛玻璃（backdrop blur） | 缺陷 #1 的修复载体；API<31 降级策略与现有 AppBlur 一致 |
| 2 | CardGroup 式设置分块（外圆角 20dp/内 4dp 分段卡 + 按压时条目圆角弹性形变；参考 rikkahub 思路，AGPL 零复制） | 设置页「原生系统设置式」分组；配合二级子页（设置项 >15 个按 M3 settings 模式拆分，副标题显示当前值） |
| 3 | LoadingIndicator 形变加载（整页首载）/ CircularWavyProgressIndicator（按钮内 pending）/ LinearWavyProgressIndicator（未来 T16 Diff、T22 上传进度） | 替换 15 处裸 CircularProgressIndicator 的分场景方案 |
| 4 | SegmentedButton 替换全部单选组：主题模式、编辑器编辑/预览、图标风格、（T16）unified/side-by-side | M3 单选标准件 |
| 5 | SwipeToDismissBox 通知左右滑（右滑标已读 reset 回弹 / 左滑删除，背景随 progress 渐变）+ animateItem 补位 | 配合通知面板票 |
| 6 | NavHost 全局转场（enter slideX(it/4)+fadeIn EmphasizedDecelerate 400ms；popExit scaleOut(0.9)）+ manifest `enableOnBackInvokedCallback="true"`（预测返回；material3 1.3+ 的 ModalBottomSheet/SearchBar 自动获得手势动画） | §4.1 落地 + 手势现代化 |
| 7 | 共享元素转场：列表头像→详情头像 sharedElement、仓库卡→RepoHeader sharedBounds | 导航连续性 |
| 8 | MotionScheme.expressive() 作为动效运行时载体 + LocalMotionScale（DataStore × 系统 ANIMATOR_DURATION_SCALE 取最小）统一缩放 | 复活 motionScale 死设置；尊重系统「减弱动画」（§4.4 目前未实现）；spatial/effects spec 分类原则 |
| 9 | 共享组件族立项：AppEmptyState / AppErrorState / AppLoadingState / AppStateChip（含状态色语义表 open→success、closed→danger、merged→tertiary、draft→surfaceVariant）/ AppListItem / RelativeTime 上移 core:ui | 消灭 Home/Notifications/Profile 三套手搓空错载态；统一时间显示（缺陷 #11） |
| 10 | dropShadow()/innerShadow() 精细阴影（Foundation 1.9+）：图片查看器圆角卡外投影、代码块/WebView 容器内凹质感 | §1.1.4「阴影精细均匀」的补全（tonal elevation 表达不了的场景）；颜色走 scrim+alpha 不违反红线 |
| 11 | 主题色低对比渐变（Brush.verticalGradient/radialGradient，primaryContainer→surface 低 alpha）：Login/GuestWelcome hero 区、Trending 大卡、空态插画背景 | FEEDBACK #30 背景美化的轻量兜底（与背景图方案不冲突，作无图用户默认） |
| 12 | AnimatedContent + SizeTransform(clip=false) 容器尺寸编排 | Issue 头部状态变化、通知分组折叠的高度平滑过渡 |
| 13 | 技术债批次：字符串路由→@Serializable 类型安全路由（navigation 2.8 已支持）；itemKey 迁移（缺陷 #6）；@Immutable 批量标注（缺陷 #7）；核心组件补 Light/Dark Preview（缺陷 #16） | API 现代化 + 性能 + 迭代效率 |
| 14 | 版本策略备忘：主体锁 material3 1.4 stable；Expressive 组件（LoadingIndicator/Wavy/FlexibleTopAppBar/SplitButton/FloatingToolbar/ButtonGroup/FAB Menu）在 1.5.0-alpha 线、@ExperimentalMaterial3ExpressiveApi 门控，按需 pin alpha 覆盖 BOM 混用 | 引入 §4 各 Expressive 提案时的依赖前提 |

---

## 5. 依据与参考

- 项目内：`docs/ui-design.md`（§2 导航 / §3 页面矩阵 / §4 动效 / §5 图标 / §6 玻璃 / §7 主题 / §10 落地映射 / §11 待确认点）、`FEEDBACK.md`（#5/#6/#9/#10/#11/#17/#22/#23/#24/#28-31）、`CONTEXT.md`（卡片风格/语言点原色拍板）、`docs/adr/`（0004/0006/0007）
- 外部：m3.material.io（Expressive 方向、settings 模式）、developer.android.com（SegmentedButton、SwipeToDismissBox、predictive back、shared elements、BOM mapping）、chrisbanes/haze（backdrop blur）
- Skills（`.agents/skills/`，本次体检规则来源）：android-jetpack-compose（Google 官方）、compose-expert（aldefy，~550★）、compose-ui / compose-navigation / android-accessibility / compose-performance-audit（new-silvermoon 精选）
- 本地参照：`~/dev/rikkahub`（CardGroup 分组卡 + LargeFlexibleTopAppBar 实证；AGPL-3.0 仅参考思路零复制）、`~/dev/PiliPlus`（卡片主题色点缀参照）、`~/dev/XMSLEEP`（MD3 动态色）

## 6. 维护规则

- 本文件是 2026-08-21 审查的**静态快照**，不随修复进度滚动更新；各项修复立项后以 ticket 为准，闭环后在 ticket 关闭时回链本文件编号。
- 开发排期、波次划分、验收标准**另行决定**后另立文档（建议 `docs/ui-fix-plan.md`），不回填本文件。
